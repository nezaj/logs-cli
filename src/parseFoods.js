function parseFoods (filePath) {
  // Initialize dictionary of foods keyed into info
  // Identify blocks of diet info and read them as strings
  // For each block
    // Split string by newlines
    // For each line
      // Parse foods
      // Add foods to dictionary

  // Return dictionary formatted as string
  return filePath
}

module.exports = parseFoods;
