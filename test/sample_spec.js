/* Sample mocha test to make sure all is well */
import assert from 'assert'

describe('sample:', () => {
  context('assert(true) --', () => {
    it('works!', () => {
      assert(true)
    })
  })
})
