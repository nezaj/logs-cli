/* Sample mocha test to make sure all is well */
import assert from 'assert'

describe('Sample test:', () => {
  context('Assert true', () => {
    it('works!', () => {
      assert(true)
    })
  })
})
